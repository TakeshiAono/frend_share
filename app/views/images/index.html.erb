<%= link_to "ログアウト", new_session_path%>
<h1><%= @current_user.name %>のマイページです</h1>

<%= link_to "投稿する", new_feed_path  %>


  <table>
    <tr>
      <th><%= :coment %></th>
      <th><%= :image %></th>
      <th><%= :updated_at %></th>
      <th><%= :created_at %></th>
      <th colspan="3"><%= :詳細 %></th>
    </tr>
<%# <%= form_with(model: @images, local: true, url: users_path) do |form| %> %>
  <% @images.each do |image| %>
    <tr>
      <td><%= image[:coment]%></td>
      <td><%= image[:image]%></td>
      <td><%= image[:created_at]%></td>
      <td><%= image[:updated_at]%></td>
      <td><%= link_to "編集", edit_feed_path(image.id)%></td>
      <td><%= link_to "詳細表示", feed_path(image.id)%></td>
      <td><%= link_to "削除", feed_path(image.id), method: :delete %></td>
    </tr>
  <% end %>
  </table>
<%# <% end %> %>