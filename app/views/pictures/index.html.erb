<%= link_to "ログアウト", new_session_path%>
<h1><%= @current_user.name %>のマイページです</h1>

<%= link_to "投稿する", new_picture_path  %>
  <table>
    <tr>
      <th><%= '投稿者' %></th>
      <th><%= :coment %></th>
      <th><%= :image %></th>
      <th><%= :updated_at %></th>
      <th><%= :created_at %></th>
      <th colspan="3"><%= :詳細 %></th>
    </tr>
<%# <%= form_with(model: @images, local: true, url: users_path) do |form| %> %>
  <% @feeds.each do |feed| %>
    <tr>
      <td><%= feed.user.name%></td>
      <td><%= feed[:coment]%></td>
      <td><%= feed[:image]%></td>
      <td><%= feed[:created_at]%></td>
      <td><%= feed[:updated_at]%></td>
      <td><%= link_to "編集", edit_feed_path(feed.id)%></td>
      <td><%= link_to "詳細表示", feed_path(feed.id)%></td>
      <td><%= link_to "削除", feed_path(feed.id), method: :delete %></td>
    </tr>
  <% end %>
  </table>
<%# <% end %> %>